#Task 2
#Q1: Write a Python program to calculate the area of a circle given its radius using the formula area=π×r^2 ( Take pie as 3.14)
r= float(input("Enter The radius: \n"))
area= 3.14*r**2
print("Area of the circle is:",area)

#Q2: Create a program that takes two numbers as input and prints whether the first number is greater than, less than, or equal to the second number.
first=float(input("Enter the first number:\n"))
second=float(input("Enter the second number:\n"))

if first > second:
    print("First number is greater than Second number.")
elif first < second:
    print("First number is less than Second number.")
else:
    print("First number is equal to Second number.")

# Q3: Use the ternary operator to find the maximum of three numbers entered by the user.
nums = input("Enter the three numbers with comma separation (e.g., 5,8,3): ")
num_list = nums.split(",")

a, b, c = float(num_list[0]), float(num_list[1]), float(num_list[2])

max_num = (a if a > c else c) if a > b else (b if b > c else c)
print("Maximum is:", max_num)

#Q4:Develop a Python script that calculates the square and cube of a given number.

num = float(input("Enter the number: \n"))
print(f"Square of the {num} :{num**2} and Cube of {num} : {num**3} ")
